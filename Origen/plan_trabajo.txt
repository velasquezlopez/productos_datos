tabla transaccional de casos
	extraer el identificador (nickname) de cada robot del campo resumen
		.query("resumen.str.startswith('RPA')")
		.apply(lambda x: x[:x.find("-") - 1])

	identificar el síntoma del caso (eliminar símbolo '?' de este campo)
		

	% casos urgencia inmediata
	% casos urgencia puede esperar

	***días hábiles = fecha_resuelto - fecha_apertura (teniendo en cuenta tabla de días no hábiles)
	***cumplimiento = días hábiles reales vs. días hábiles del ANS según la urgencia


(CSVs)
assistants
roles
runners
	# máquinas
	bia

tabla de síntomas (CSV)
	listado de síntomas

tabla histórico de ftes (CSV)
	promedio ftes últimos 3 meses